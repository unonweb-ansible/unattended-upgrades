---
unattended_upgrades:
  apt:
    state: present
  config:
    src:
    dst: /etc/apt/apt.conf.d/50unattended-upgrades
  apt_daily_upgrade:
    service:
      override: # --> /etc/systemd/system/apt-daily-upgrade.service.d/override.conf
      enabled: true
    timer:
      override: # --> /etc/systemd/system/apt-daily.timer.d/override.conf
      enabled: false
  apt_daily:
    service:
      override: # --> /etc/systemd/system/apt-daily.service.d/override.conf
      enabled: true
    timer:
      override: # --> /etc/systemd/system/apt-daily.timer.d/override.conf
      enabled: true
  listchanges:
    apt:
      state: present
    config:
      src: